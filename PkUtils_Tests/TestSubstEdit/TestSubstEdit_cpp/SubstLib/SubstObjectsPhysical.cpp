// SubstObjectsPhysical.cpp 
//

/////////////////////////////////////////////////////////////////////////////
// INCLUDE FILES
/////////////////////////////////////////////////////////////////////////////
#include "stdafx.h"

/////////////////////////////////////////////////////////////////////////////
// MANIFESTED CONSTANTS & MACROS
/////////////////////////////////////////////////////////////////////////////
#ifdef _DEBUG
#define new DEBUG_NEW
#undef THIS_FILE
static char THIS_FILE[] = __FILE__;
#endif

/////////////////////////////////////////////////////////////////////////////
// PRIVATE SYMBOLS
/////////////////////////////////////////////////////////////////////////////

/////////////////////////////////////////////////////////////////////////////
// PUBLIC FUNCTIONS
/////////////////////////////////////////////////////////////////////////////

CString WINAPI extractSubstr(
    LPCTSTR szStr, 
    size_t  istart, 
    size_t   ilen)
{
    CString    strLeft, strRight;
    CString    strTmp = szStr;
#if _DEBUG
    size_t nLength = strTmp.GetLength();
    ASSERT(nLength >= istart + ilen);
#endif
    strLeft  = strTmp.Left((int)istart);
    strRight = strTmp.Right((int) (strTmp.GetLength() - (istart + ilen)));
    strTmp = strLeft + strRight;

    return strTmp; 
}

