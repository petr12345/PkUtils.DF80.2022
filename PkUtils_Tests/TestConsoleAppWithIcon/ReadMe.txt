The project TestConsoleAppWithIcon demonstrates the functionality of dynamically setting 
and updating the console window icon and taskbar icon in a Windows console application.

Features:
------------
- Loads icons from embedded resources (byte array format).

- Sets the console window icon using native Windows API calls.

- Sets the taskbar icon using NotifyIcon.

- Allows cycling through different icons dynamically on key press.